var numb = [1, 2, 3, 4, 5];

// Accessing an invalid index (index out of bounds)
var Inde = 10;
console.log(numb[Inde]); // Intentional error

console.log("Program continues...");

// Corrected version:
// Accessing a valid index
var Ind = 2;
console.log(numb[Ind]);

console.log("Program ends.");